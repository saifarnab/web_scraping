Instructions:

1. Create and acivate virual env
2. Install pip if not installed
3. Install all the dependencies
    a. pip install selenium
    b. pip install pySmartDL
    c. pip install requests
4. Change the OUTPUT_FOLDER_PATH variable in code with your destination folder path
5. Run > python tainio.py
6. Do not close the automated browser
7. If 'Website have blocked you IP, please solve the captcha.' this message generates in console,
    open the automate brower go to mobile version & solve the captcha & click submit.
     Minimize the browser, script will take care the rest.
